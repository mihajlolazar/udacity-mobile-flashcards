import { StyleSheet } from 'react-native';

export const container = StyleSheet.create({
  default: {
    paddingTop: 15,
    paddingLeft: 20,
    paddingRight: 20,
    paddingBottom: 20,
    flex: 1,
  }
});